# PartSouq Catalog 最終交接文件

更新時間：2026-08-23 13:00（Asia/Taipei）  
正式專案路徑：`/Users/a861252012/Desktop/folder/code/partsouq-catalog`  
Git：`main` / `692076ea983ef6d10a0a2fd30ba08caed357af16`  
Remote：`git@github.com:a861252012/partsouq-catalog.git`

## 一句話狀態

程式碼、migration、後台與本機排程安全閘門已完成大部分；但正式需求仍未驗收完成。
主 DB 目前只有 10,000 筆 raw PartSouq `parts`，沒有任何已發布 PartSouq 資料、
NHTSA VIN decode 或已確認 VIN↔PartSouq mapping。

## 目前可確認的事實

### Git 與本機服務

- `HEAD == origin/main == 692076e`，工作樹乾淨。
- MySQL、FastAPI admin、station-admin、queue-scheduler 容器正在執行且 healthy。
- 本機連線：MySQL `127.0.0.1:3308`、admin `127.0.0.1:8000`、
  station-admin `127.0.0.1:8086`。
- catalog 與 NHTSA 正式 scheduler 目前沒有執行；不能同時啟動 host catalog
  LaunchAgent 與 Compose catalog scheduler。
- 專案沒有 deploy production。`.github/workflows/ci.yml` 是測試流程，不是部署。
  最新 GitHub Actions 紀錄仍是 `394fda8` 的失敗；後續本機修正 commit 使用
  `[skip ci]`，避免繼續消耗 Actions。未使用付費 proxy、付費 browser、付費 runner
  或其他付費服務。
- repository 內已找不到 `ted-Lin-Bibian` 字串。

### 主 DB `partsouq_catalog` 唯讀盤點

| 項目 | 筆數／狀態 |
|---|---:|
| `parts` raw fitment rows | 10,000 |
| `bounded_parts` | 0 |
| `published_parts` | 0 |
| `v_current_catalog_parts` | 0 |
| `part_quarantine` | 2,260 |
| `nhtsa_vin_decodes` | 0 |
| `station_admin_vin_vehicle_mappings` | 0 |
| `station_admin_vin_part_fitments` | 0 |

最近 PartSouq run 是 `crawl_runs.id=5`：

- `status=error`
- `target_parts=10000`
- `parts_ok=10000`
- `brands/models/vehicles/groups=1/1/2/2`
- `evidence_status=collecting`
- `evidence_artifact_count=0`
- `evidence_record_count=0`
- `run_key=bounded-10000-s260823113430443`

所以「raw parts 恰好 10,000」不是成功。它沒有通過 bounded snapshot、evidence seal、
publish 與正式 view gate，後台顯示沒有已發布資料是正確且必要的 fail-closed 行為。

NHTSA 另有 `nhtsa_sync_runs.id=1,2` 殘留 `running`，對應 scheduler job 已失敗
（exit 124／125）。它們尚未產生 artifact、record 或 VIN decode。

### Migration

- 程式 manifest 已到 catalog migration `023`。
- 主 DB ledger 目前只套到 `migration:022`。
- 主 DB 尚無 `partsouq_http_diagnostics`；這是刻意保留，避免未提交版本先污染正式 DB。
- `partsouq_catalog_test` 已驗過 migration 023 checksum
  `83951039e522c3bdf9caf887bd46ebf65594649f4ac57b4810a35a140da08918`。

## 已完成的主要修改

### PartSouq

- macOS Aqua LaunchAgent 本機正式排程；固定使用既有 `partsouq_catalog` 與 bounded
  10,000，拒絕 sample、`_test` DB、付費 CloakBrowser、proxy、自訂 binary／TLS。
- Chromium owner、一次性 readiness marker、cookie／refresh lock、回收與 heartbeat
  已收斂，避免 scheduler 測試直接在非 Aqua 環境啟動 GUI 造成 macOS crash alert。
- bounded 10,000 發布要求 daemon provenance、精確筆數、六種 live HTTP evidence、
  replay、hash、sanitizer version、scheduler attempt 與 DB snapshot 全部一致。
- quarantine 後台、處置、總筆數、篩選、分頁與可選 pageSize 已接通 FastAPI 與
  station-admin。
- part／vehicle／category／group 的站方識別欄位已保留：`part_number`、`code`、
  `vehicle_vid`、`category_cid`、`group_uid`。`code` 是零件表 Code，不是車型 ID。
- 最新 `692076e` 修正 bounded late-worker 競態；正式 404 必須帶可核對的 response
  envelope；HTTP 200 空解析維持 fail-closed；404／空解析會保存有界、去敏且不混入
  正式 evidence 的 diagnostic。

### NHTSA／VIN

- 已有 NHTSA bulk／allowlist API、artifact／record lineage、scheduler 與 VIN queue 架構。
- 已修正長任務 heartbeat、重複驗證與 scheduler silent-stall 誤殺的一部分問題。
- VIN 只允許處理使用者提供或有權使用的 17 碼 VIN；禁止枚舉 VIN。

### 後台

- FastAPI admin 與原站方 station-admin 都可本機啟動，health 會檢查 DB readiness，
  不是只看 HTTP 200。
- PartSouq、quarantine、VIN decode、VIN↔PartSouq mapping 與 fitment 都有對應 schema／
  頁面路徑；目前畫面空白的直接原因是正式發布與 mapping 都是 0 筆。

## 本輪實際驗證紀錄

`692076e` 提交前已實跑並通過：

- Ruff check／format。
- `mypy --strict`：本輪三個異動 source modules。
- PartSouq HTTP compliance、bounded、migration focused tests。
- migration 023 fresh replay 與重跑。
- MySQL inline diagnostic upsert、隔離、壓縮還原與去敏檢查。
- `git diff --check`。
- 獨立 adversarial review：沒有剩餘 P0／P1。

依使用者最後指示，本輪沒有重跑完整 E2E。舊紀錄中的 `672 passed` 是較早 commit
的完整 gate，不能拿來證明 `692076e` 全套 E2E 已綠。

## 尚未完成的需求與優先順序

### P0：正式 PartSouq 10,000 驗收

1. 在沒有其他 writer／scheduler 的前提下，從乾淨 commit 套用 migration 023。
2. 用 `deploy/install-macos-catalog-scheduler.zsh` 產生 immutable release，透過 Aqua
   LaunchAgent 執行，禁止直接從 Codex sandbox／SSH 啟動 headed Chromium。
3. 讓 scheduler 自動重試失敗 scope；不可人工改 DB、補資料或把 diagnostic 當 evidence。
4. 若同一 unit 回 HTTP 404，須由 response envelope 證明；若 HTTP 200 仍零解析，必須
   修 parser／來源契約後重跑，不能標成 `not_found`。
5. 成功標準必須同時成立：
   - scheduler `completed` 且 exit 0；
   - crawl `bounded_success`；
   - `bounded_parts=published_parts=v_current_catalog_parts=10000`；
   - evidence `verified`，manifest／dataset hash、artifact／record／bytes 都非零；
   - 10,000 筆逐筆有真實 PartSouq lineage；
   - quarantine 有紀錄但不混入正式發布列。

### P0：NHTSA 真實資料與 VIN mapping

1. 先修復／回收 `nhtsa_sync_runs.id=1,2` 的 stale `running`，並補強 MySQL lease、
   publish 原子性與中斷 rollback。
2. 重新執行 NHTSA bulk／allowlist API，必須產生 current artifact／record；不能只看
   HTTP 200 或 scheduler started。
3. 取得使用者明確提供或授權的 VIN 清單。沒有授權 VIN 時不可自行枚舉，所以目前無法
   完成 VIN decode 驗收。
4. 對每個 VIN 保存 `Make`、`Model`、`Year`、`Engine`、`Displacement/Trim`、VIN，
   再建立 confirmed VIN↔PartSouq vehicle mapping。
5. 由 mapping 產生 VIN↔part fitment，抽查 part number、英文名稱、品牌、車型、年份／
   生產期間、分類與站方 ID 都能回溯來源。

### P1：正式資料後台 E2E

- 用已發布的真實 10,000 筆測 admin 與 station-admin：總筆數、pageSize、首／末／上一／
  下一頁、搜尋、分類、料件編輯、quarantine resolve、VIN decode、mapping、fitment。
- UI 與 API 需交叉核對 DB；不能以畫面載入成功取代資料正確性。

### P2：已知測試技術債

- migration 023 postflight 尚未逐欄驗型別／nullability，也未驗 unique index 欄位順序；
  建議補 exact-schema mutation test。
- 404 rollback 現有 unit test 只讓第一個 terminal SQL 失敗；建議加 MySQL integration，
  讓 artifact supersede、membership clear 之後故意讓 receipt 失敗，再確認整段 rollback，
  但先前獨立 commit 的 diagnostic 仍存在。

## 下一個 agent 的安全起手式

```bash
cd /Users/a861252012/Desktop/folder/code/partsouq-catalog
git status --short
git rev-parse HEAD
git rev-parse origin/main
docker compose ps
launchctl list | rg 'partsouq|nhtsa'
```

套 migration 前，先唯讀確認主 DB ledger、所有 writer 與排程狀態。確認可寫後才執行：

```bash
set -a
source .env
set +a
.venv/bin/partsouq-catalog-migrate apply
.venv/bin/partsouq-catalog-migrate check
```

正式 catalog 應使用 installer／LaunchAgent，不要直接執行 Chromium：

```bash
deploy/install-macos-catalog-scheduler.zsh --no-start
deploy/install-macos-catalog-scheduler.zsh
launchctl print "gui/$(id -u)/com.partsouq.catalog-scheduler"
```

不要刪除 `partsouq_catalog`、Docker volume、歷史 crawl／scheduler／evidence／quarantine
資料。任何刪除或重建都要先列出精確目標、影響與可復原性，取得使用者同意。

